/* Calculate total balloons needed:
  1. Add original guests (15) and new guests (9) first.
  2. Multiply the total group size by 2 balloons per person.
*/
(15 + 9) * 2; // Returns 48




/* Calculating total balloons using BODMAS rules:
  
  B - Brackets ( )   <-- Priority 1: (15 + 9) happens first
  O - Orders **
  D - Division /
  M - Multiplication * <-- Priority 2: Result * 2 happens second
  A - Addition +
  S - Subtraction -
*/

(15 + 9) * 2; // Returns 48